---
description: MacDevClean engineering contract
alwaysApply: true
---

Read AGENTS.md and the active plan before editing.

`AGENTS.md` is the canonical engineering contract: Swift 6, macOS 14, native
SwiftUI, inward package dependencies, Trash-only filesystem cleanup, validated
cleanup plans, test-owned fixtures with fake destructive adapters, Conventional
Commits and `main -> develop -> feat/*` with `--no-ff` merges.

Execution order and the branch lifecycle are defined in
`docs/superpowers/plans/2026-09-21-00-execution.md`. Current state is tracked in
`docs/progress.md`.

Never clean the user's real machine, run the reference shell script, publish to
a remote, or invent test results.
